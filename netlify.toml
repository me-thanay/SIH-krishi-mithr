[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  # Disable Python detection for Next.js build
  PYTHON = ""
  PYTHON_VERSION = ""
  # Disable Nixpacks Python detection
  NIXPACKS_NO_PYTHON = "1"
